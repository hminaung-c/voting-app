apiVersion: v1
kind: Pod
metada:
  name: postgres-pod
  labes:
    name: postgres-pod
    app: demo-voting-app
spec:
  containers:
    - name: postgres
      image: postgres
      ports:
        - containerPort: 5432